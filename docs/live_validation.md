# Live 联调结果

本次 live 联调使用场景：`Cityscape`

执行脚本：
- `tests/live_smoke.py`

执行方式：
- 通过 `automation/run_with_timeout.py` 启动，确保 Quanser 配置和超时控制一致。

## 1. 实测结果

- 采样帧数：`8`
- 平均检测目标数：`19.5`
- 告警帧数：`0`
- 紧急制动帧数：`0`
- 平均单帧处理时间：`0.0047565 s`
- 最大单帧处理时间：`0.0051285 s`

## 1.1 可视化联调结果

执行脚本：
- `tests/live_visual_demo.py`

输出文件：
- 摘要：`docs/live_visual_summary.json`
- 抓图：`docs/captures/live_visual_demo_Obstacle_Detection_Live.png`

实测结果：
- 采样帧数：`20`
- 平均检测目标数：`19.75`
- 告警帧数：`9`
- 紧急制动帧数：`9`
- 平均单帧处理时间：`0.0125127 s`
- 最大单帧处理时间：`0.0151191 s`
- GUI 是否开启：`True`

## 2. 结果说明

- 新工程骨架已经能够在真实 QLabs `Cityscape` 场景中完成：
  - 场景启动
  - QCar 生成
  - 激光雷达数据读取
  - 点云预处理与聚类
  - 障碍物列表输出
  - 决策接口计算
- 在可视化联调中，已经能够显示：
  - 极坐标占据图
  - 局部占据图
  - 全局占据图
  - 目标点云
  - 障碍物框和中心点
- 当前场景中没有进入紧急制动判定区域，因此 `warning` 和 `emergency_brake` 均为 `0`。
- 在更长帧数的可视化联调中，系统已经出现 `warning` 和 `emergency_brake` 判定，说明风险输出链路已经触发。
- 实测单帧耗时仍显著低于提纲中的 `33 ms` 实时性目标。

## 3. 当前状态判断

从系统链路角度，课题已经完成以下关键闭环：

- `场景 -> 传感器 -> 坐标转换 -> 聚类检测 -> 跟踪 -> 风险判断`

下一阶段应继续补强：

- 检测框和局部/全局地图统一可视化
- 更严格的多场景精度验证
- 动态目标 KF 跟踪
- 与制动/绕障控制的真实闭环联调
